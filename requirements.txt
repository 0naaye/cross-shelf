# 융합형 테마 서가 발굴 — 실행에 필요한 패키지
#
# 설치:  pip install -r requirements.txt
#
# 아래 버전은 제출본을 실제로 재실행해 확인한 환경(Windows 11 / Python 3.14.3)의
# 버전 그대로입니다.
pandas==3.0.3
requests==2.34.2
networkx==3.6.1
matplotlib==3.10.8
python-dotenv==1.2.2

# 파이썬 버전이 달라 위 설치가 실패하면, 하한만 지정해 설치하셔도 동작합니다.
#   pip install "pandas>=2.0" "requests>=2.28" "networkx>=3.1" "matplotlib>=3.7" "python-dotenv>=1.0"
#
# networkx 는 4단계(06_build_clusters.py)의 Louvain 커뮤니티 탐지에,
# matplotlib 은 그림 1·2(13·14)에 쓰입니다.
